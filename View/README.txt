View

The view is the representation of the user interface. Usually there are many (possibly nested) views in a single application.
A view can query the model, but it is not supposed to change the state. In web based MVC systems, a view can be implemented
using a template that renders an HTML page.